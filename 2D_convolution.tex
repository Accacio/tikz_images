\definecolor{N7blue}{HTML}{104F9E}
\begin{tikzpicture}
  \def\a{{
      {2,     2,     1,     5,     4},
      {4,     3,     1,     1,     6},
      {6,     4,     3,     1,     0},
      {0,     5,     5,     3,     2},
      {2,     0,     5,     4,     3}}}
  \def\b{{
      {0,    -1,     0},
      {-1,     5,    -1},
      {   0,    -1,     0}}}
  \def\c{{
      { 4 ,     4,    -3,    19,     9},
      {  9,     4,    -3,    -8,    25},
      { 22,     3,     4,    -2,    -9},
      {-13,    16,     9,     3,     4},
      { 10,   -12,    16,     9,     9}}}
  \begin{scope}[yslant=-.25,xshift=10cm,yshift=6cm]
    \fill[N7blue!50] (1,1) rectangle +(1,1);
    \draw[N7blue] (0,0) grid (5,5);

    \foreach \i in {0,...,4}
    \foreach \j in {0,...,4}{
      \pgfmathsetmacro{\x}{int(\c[\i][\j])}
      \path (\i+.5,\j+.5) node{\x};}

    \path (1+.5,1+.5) node[inner sep=0.25cm] (solution) {};

    \path
    (1,1) coordinate (C1)
    (1,2) coordinate (C2)
    (2,2) coordinate (C3)
    (2,1) coordinate (C4);

    \node[yslant=-.25] at (2.5,6) {Image filtrée};
  \end{scope}

  \begin{scope}[yslant=-.25,xshift=6cm,yshift=3cm]
    \path
    (0,0) coordinate (B1)
    (0,3) coordinate (B2)
    (3,3) coordinate (B3)
    (3,0) coordinate (B4);
  \end{scope}

  \draw[dashed, N7blue!30!white] (B1) to (C1);
  \draw[dashed, N7blue!30!white] (B2) to (C2);
  \draw[dashed, N7blue!30!white] (B3) to (C3);
  \draw[dashed, N7blue!30!white] (B4) to (C4);

  \begin{scope}[yslant=-.25,xshift=6cm,yshift=3cm]
    \fill[N7blue!20] (0,0) rectangle +(3,3);
    \fill[N7blue!50] (1,1) rectangle +(1,1);
    \draw[N7blue] (0,0) grid (3,3);
    % \draw[blue,very thick] (0,0) rectangle (4,4);
    % \foreach \i in {0,...,3}
    % \foreach \j in {0,...,3}{
    % \pgfmathsetmacro{\x}{int(\a[\i][\j])}
    % \path (\i+.5,\j+.5) node{\x};}
    % \path (Ori.north) node[above=2mm]{Original};
    \foreach \i in {0,...,2}
    \foreach \j in {0,...,2}{
      \pgfmathsetmacro{\x}{int(\b[\i][\j])}
      \path (\i+.5,\j+.5) node{\x};}


    \node[yslant=-.25] at (1.5,4) {Noyau};
  \end{scope}


  \begin{scope}[yslant=-.25]
    \path
    (0,0) coordinate (A1)
    (0,3) coordinate (A2)
    (3,3) coordinate (A3)
    (3,0) coordinate (A4);
  \end{scope}

  \draw[dashed, N7blue!40!white] (A1) to (B1);
  \draw[dashed, N7blue!40!white] (A2) to (B2);
  \draw[dashed, N7blue!40!white] (A3) to (B3);
  \draw[dashed, N7blue!40!white] (A4) to (B4);

  \begin{scope}[yslant=-.25]
    \fill[N7blue!20,opacity=0.7] (0,0) rectangle +(3,3);
    \fill[N7blue!50,opacity=0.7] (1,1) rectangle +(1,1);
    \draw[N7blue] (0,0) grid (5,5);
    % \draw[blue,very thick] (0,0) rectangle (4,4);
    \foreach \i in {0,...,4}
    \foreach \j in {0,...,4}{
      \pgfmathsetmacro{\x}{int(\a[\i][\j])}
      \path (\i+.5,\j+.5) node{\x};}

    \node[yslant=-.25] at (2.5,6) {Image originale};
  \end{scope}

  \draw node (equation) at ($(solution)+(2.,-5)$)  {$4 =1\times 0 +1\times (-1) +3\times 0 + 2\times (-1) +3\times 4 + 4\times (-1) + 2\times 0 + 4\times (-1) +6\times 0$ };

  \draw[-{latex}] (solution) -- (equation);
\end{tikzpicture}
